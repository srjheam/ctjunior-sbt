import { Button, Center, Flex } from '@chakra-ui/react'
import type { NextPage } from 'next'

const Home: NextPage = () => {
  return (
    <Center h={'100vh'}>
      <Button>
        Button
      </Button>
    </Center>
  )
}

export default Home
